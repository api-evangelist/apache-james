naftiko: 1.0.0-alpha2
info:
  label: Apache James WebAdmin REST API — MailRepositories
  description: 'Apache James WebAdmin REST API — MailRepositories. 1 operations. Lead operation: Apache james Apache James
    WebAdmin List Mail Repositories. Self-contained Naftiko capability covering one Apache James business surface.'
  tags:
  - Apache James
  - MailRepositories
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    APACHE_JAMES_API_KEY: APACHE_JAMES_API_KEY
capability:
  consumes:
  - type: http
    namespace: webadmin-rest-mailrepositories
    baseUri: http://localhost:8000
    description: Apache James WebAdmin REST API — MailRepositories business capability. Self-contained, no shared references.
    resources:
    - name: mailRepositories
      path: /mailRepositories
      operations:
      - name: listmailrepositories
        method: GET
        description: Apache james Apache James WebAdmin List Mail Repositories
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.APACHE_JAMES_API_KEY}}'
  exposes:
  - type: rest
    namespace: webadmin-rest-mailrepositories-rest
    port: 8080
    description: REST adapter for Apache James WebAdmin REST API — MailRepositories. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/mailrepositories
      name: mailrepositories
      description: REST surface for mailRepositories.
      operations:
      - method: GET
        name: listmailrepositories
        description: Apache james Apache James WebAdmin List Mail Repositories
        call: webadmin-rest-mailrepositories.listmailrepositories
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: webadmin-rest-mailrepositories-mcp
    port: 9090
    transport: http
    description: MCP adapter for Apache James WebAdmin REST API — MailRepositories. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: apache-james-apache-james-webadmin
      description: Apache james Apache James WebAdmin List Mail Repositories
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: webadmin-rest-mailrepositories.listmailrepositories
      outputParameters:
      - type: object
        mapping: $.
