naftiko: 1.0.0-alpha2
info:
  label: Apache James WebAdmin REST API — MailQueues
  description: 'Apache James WebAdmin REST API — MailQueues. 1 operations. Lead operation: Apache james Apache James WebAdmin
    List Mail Queues. Self-contained Naftiko capability covering one Apache James business surface.'
  tags:
  - Apache James
  - MailQueues
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    APACHE_JAMES_API_KEY: APACHE_JAMES_API_KEY
capability:
  consumes:
  - type: http
    namespace: webadmin-rest-mailqueues
    baseUri: http://localhost:8000
    description: Apache James WebAdmin REST API — MailQueues business capability. Self-contained, no shared references.
    resources:
    - name: mailQueues
      path: /mailQueues
      operations:
      - name: listmailqueues
        method: GET
        description: Apache james Apache James WebAdmin List Mail Queues
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.APACHE_JAMES_API_KEY}}'
  exposes:
  - type: rest
    namespace: webadmin-rest-mailqueues-rest
    port: 8080
    description: REST adapter for Apache James WebAdmin REST API — MailQueues. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/mailqueues
      name: mailqueues
      description: REST surface for mailQueues.
      operations:
      - method: GET
        name: listmailqueues
        description: Apache james Apache James WebAdmin List Mail Queues
        call: webadmin-rest-mailqueues.listmailqueues
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: webadmin-rest-mailqueues-mcp
    port: 9090
    transport: http
    description: MCP adapter for Apache James WebAdmin REST API — MailQueues. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: apache-james-apache-james-webadmin
      description: Apache james Apache James WebAdmin List Mail Queues
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: webadmin-rest-mailqueues.listmailqueues
      outputParameters:
      - type: object
        mapping: $.
